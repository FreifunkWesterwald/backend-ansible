- name: install radvd
  apt:
      name: radvd
      update_cache: yes
- name: radvd config
  template:
      src: radvd.conf
      dest: /etc/radvd.conf

- name: start and enable radvd
  systemd:
      name: radvd.service
      enabled: yes
      state: started
